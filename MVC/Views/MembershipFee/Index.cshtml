@model IEnumerable<CoreMvc.Models.MembershipFee>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MembershipName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fee)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MembershipName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fee)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MembershipType">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MembershipType">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MembershipType">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
